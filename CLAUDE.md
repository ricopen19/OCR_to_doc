# プロジェクト固有ルール

Unlimited-OCR-GGUF 導入 → 暴走生成ハルシネーション頻発 → glm-ocr 単体構成へ撤去、という回り道から得た教訓。

## OCR モデルの変更・追加

- **モデル導入前に実際の不具合報告をリサーチする**: 公称スペック（パラメータ数等）を鵜呑みにせず、GitHub Issue 等で既知の不具合（ハルシネーション・反復生成等）が報告されていないか事前に確認する。Unlimited-OCR-GGUF は公称「0.9B」だったが実際は2.93Bで、長文生成時の反復ハルシネーションもアーキテクチャ由来の既知の欠陥として複数報告されていた
- **要素ごとに複数モデルを混在させる構成は避け、まず単一モデルで足りないか検証してから検討する**: 本文と表でモデルを使い分けるような構成は、クロップ・退避・再結合といった「継ぎ目」自体が新しい失敗点になる。速度と精度を両立させたくなっても、複雑さに見合う効果があるか一度立ち止まる
- **新モデル導入・置換のような大きい変更は `feature/xxx` ブランチを切ってから実装する**（`main` を安定版に保つ）

## 検証の仕方

- **パイプラインの生成ロジックを大きく変えた直後は、ユニットテスト通過だけで「完了」と判断せず、過去に実際に問題が起きた入力データで通し実行して確認する**。カバレッジのある領域を削除・置換したときは特に、そのカバレッジが守っていた実データケースで再確認する（例: 表のフェンス重複除去ロジックを削除した際、テストは全部通ったがページ全体の重複再出力という新しいバグを実データでのみ発見した）

## OCR 精度の限界

- OCR だけでは完全な出力は作れない（特に数式・LaTeX 周りは glm-ocr 自体の生成揺らぎで壊れることがある）。恒久対応には LLM 校正フローが必要（`docs/tasks.md` に既存タスクあり、現在は優先度低で保留）
